📂 画廊页面 (Gallery) 视觉重构执行文档
改造核心理念：

视觉隐喻：将“画廊”重塑为 “创意数据库 (Creative Database)”。

交互风格：标签切换像是在切换“数据频道”，搜索框像是在输入“终端指令”。

卡片设计：不再是白色卡片，而是悬浮的 “全息切片 (Holo-Slices)”。

1. 📄 HTML 结构重构 (templates/gallery.html)
执行动作：请用以下代码替换 templates/gallery.html 中原有的 <div class="container ..."> 头部区域（包括标题、筛选和搜索栏）。 (注：请保留页面下方的 <div class="cases-grid"> 列表渲染逻辑，稍后我们会用 CSS 改变它的外观)

HTML

<div class="container mx-auto px-4 py-12 relative z-10">
    
    <div class="text-center mb-12 relative">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 text-9xl font-black text-white/5 pointer-events-none select-none tracking-widest scale-150">
            ARCHIVE
        </div>
        
        <div class="relative z-10">
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-purple-500/30 bg-purple-500/10 text-purple-300 text-[10px] font-mono mb-4 tracking-[0.2em]">
                <span class="w-1.5 h-1.5 bg-purple-400 rounded-full animate-pulse"></span>
                DATABASE_ACCESS
            </div>
            
            <h1 class="text-4xl md:text-6xl font-black text-white mb-4 tracking-tighter drop-shadow-[0_0_15px_rgba(217,70,239,0.3)]">
                灵感<span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-cyan-400">·全息库</span>
            </h1>
            
            <p class="text-gray-400 font-mono text-sm md:text-base max-w-xl mx-auto">
                [检索结果] 探索 <span class="text-cyan-400">User_Generated_Content</span> 的无限可能。
            </p>
        </div>
    </div>

    <div class="gallery-controls max-w-5xl mx-auto">
        
        <div class="search-box mb-10 flex justify-center">
            <div class="relative w-full max-w-xl group">
                <div class="absolute -inset-0.5 bg-gradient-to-r from-cyan-500 to-purple-500 rounded-full opacity-30 group-focus-within:opacity-100 transition duration-500 blur"></div>
                
                <div class="relative flex items-center bg-[#0f172a] rounded-full p-1">
                    <div class="pl-4 pr-2 text-gray-500 font-mono select-none">root@baji:~#</div>
                    <input type="text" 
                           placeholder="输入关键词检索数据..." 
                           class="search-input w-full bg-transparent text-white px-2 py-3 focus:outline-none font-mono text-sm placeholder-gray-600"
                           v-model="searchQuery" 
                           @keyup.enter="performSearch"> <button @click="performSearch" class="p-2 rounded-full bg-white/5 hover:bg-cyan-500/20 text-gray-400 hover:text-cyan-300 transition mr-1">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                    </button>
                </div>
            </div>
        </div>

        <div class="filter-tabs flex flex-wrap justify-center gap-3">
            <button class="filter-tab active px-5 py-2 rounded border border-cyan-500/50 bg-cyan-500/10 text-cyan-300 text-sm font-bold tracking-wide shadow-[0_0_15px_rgba(34,211,238,0.2)] transition-all relative overflow-hidden group">
                <span class="relative z-10">ALL_DATA</span>
                <div class="absolute inset-0 bg-cyan-400/10 transform -skew-x-12 translate-x-full group-hover:translate-x-0 transition-transform"></div>
            </button>

            <button class="filter-tab px-5 py-2 rounded border border-white/10 bg-white/5 text-gray-400 text-sm hover:border-white/30 hover:text-white transition-all hover:bg-white/10">
                RECOMMENDED
            </button>
            
            <button class="filter-tab px-5 py-2 rounded border border-white/10 bg-white/5 text-gray-400 text-sm hover:border-white/30 hover:text-white transition-all hover:bg-white/10">
                TRENDING 🔥
            </button>
            
            </div>
    </div>
</div>
2. 🎨 列表卡片样式改造 (Holo-Card Style)
画廊的核心是下方的作品列表。原有的 bg-white shadow-sm 必须被替换。

执行动作 1：修改 HTML 类名 在 templates/gallery.html 下方的列表循环部分（v-for="caseData in ..."），找到最外层的卡片容器 div，替换其类名。

原代码：

HTML

<div class="case-card group bg-white rounded-xl shadow-sm hover:shadow-xl overflow-hidden">
新代码：

HTML

<div class="holo-card group relative rounded-2xl overflow-hidden transition-all duration-300 hover:-translate-y-2">
执行动作 2：追加 CSS 样式 将以下 CSS 添加到 static/css/design.css 末尾，专门用于定义“全息卡片”。

CSS

/* === 全息档案卡片 (Holo-Archive Card) === */

.holo-card {
    background: rgba(17, 24, 39, 0.6); /* 深色半透明背景 */
    border: 1px solid rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
}

/* 悬停时的霓虹发光边框 */
.holo-card:hover {
    border-color: rgba(34, 211, 238, 0.4); /* 变亮蓝 */
    box-shadow: 0 0 30px rgba(6, 182, 212, 0.15);
}

/* 卡片内部的图片容器 */
.holo-card .case-image-container {
    position: relative;
    background: radial-gradient(circle at center, #1e293b 0%, #020617 100%); /* 替换原来的浅色背景 */
    border-bottom: 1px solid rgba(255,255,255,0.05);
}

/* 图片本身的悬停效果 */
.holo-card .case-image {
    transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
    filter: drop-shadow(0 10px 20px rgba(0,0,0,0.5));
}

.holo-card:hover .case-image {
    transform: scale(1.1) rotate(3deg); /* 悬停时轻微放大并旋转，更有动感 */
}

/* 卡片底部信息区 */
.holo-card .case-info {
    padding: 1.25rem;
}

.holo-card .case-title {
    color: #f1f5f9; /* 亮白文字 */
    font-family: var(--font-hero);
}

/* 标签样式重写 */
.holo-card .case-category {
    background: rgba(255, 255, 255, 0.05) !important;
    color: #94a3b8 !important;
    border: 1px solid rgba(255, 255, 255, 0.1);
    font-family: monospace;
    font-size: 0.75rem;
}

/* 统计数据文字 */
.holo-card .case-stats {
    color: #64748b !important;
    font-family: monospace;
}

/* 覆盖层操作按钮 (Hover Actions) */
.holo-card .case-overlay {
    background: rgba(0, 0, 0, 0.7) !important;
    backdrop-filter: blur(4px);
}

.holo-card .action-btn {
    background: rgba(255, 255, 255, 0.1) !important;
    border: 1px solid rgba(255, 255, 255, 0.2) !important;
    color: white !important;
    backdrop-filter: blur(4px);
}

.holo-card .action-btn:hover {
    background: var(--neon-cyan) !important;
    border-color: var(--neon-cyan) !important;
    color: black !important;
    box-shadow: 0 0 15px var(--neon-cyan);
}
3. ⚡️ 功能绑定检查 (Integration Guide)
为了保证**“不改变一点功能”**，请在替换 HTML 时特别注意以下 Vue 指令的保留。

在 templates/gallery.html 中修改筛选按钮组时：

原有的逻辑大概是这样的（假设）：

HTML

<button v-for="filter in filters" 
        @click="currentFilter = filter.value"
        :class="{ 'active': currentFilter === filter.value }" ...>
    {{ filter.label }}
</button>
你需要将其改为新的样式结构，但保留指令：

HTML

<div class="filter-tabs flex flex-wrap justify-center gap-3">
    <button v-for="filter in filters" :key="filter.value"
            @click="setFilter(filter.value)" 
            class="px-5 py-2 rounded border text-sm font-bold tracking-wide transition-all relative overflow-hidden group"
            :class="[
                currentFilter === filter.value 
                ? 'border-cyan-500/50 bg-cyan-500/10 text-cyan-300 shadow-[0_0_15px_rgba(34,211,238,0.2)]' 
                : 'border-white/10 bg-white/5 text-gray-400 hover:border-white/30 hover:text-white hover:bg-white/10'
            ]">
        <span class="relative z-10" v-text="filter.label"></span>
        <div v-if="currentFilter === filter.value" class="absolute inset-0 bg-cyan-400/10 transform -skew-x-12 translate-x-full group-hover:translate-x-0 transition-transform"></div>
    </button>
</div>
4. 📝 总结：执行步骤
样式注入：将 第 2 部分 的 CSS 代码复制粘贴到 static/css/design.css 的最底部。

头部替换：打开 templates/gallery.html，找到 <div class="container ..."> 包含标题和搜索框的部分，用 第 1 部分 的 HTML 代码替换。

注意： 记得把原来的 Vue 绑定（如 v-model="searchQuery"）加回到新代码的 <input> 标签上。

列表卡片微调：在 templates/gallery.html 下方，找到 v-for 循环的卡片容器，把 class 属性从 case-card ... 改为 holo-card ...。

完成这三步，你的画廊页面就会从“淘宝买家秀”变成“赛博朋克数据库”，而且所有的搜索、筛选、点击跳转功能都能完美运行。